sudo: required
services:
  - docker
language: generic

before_install:
  - docker build -t adminrobert/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true adminrobert/docker-react npm run test
